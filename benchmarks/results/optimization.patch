--- a/src/zstd/common.nim
+++ b/src/zstd/common.nim
@@ -1,6 +1,6 @@
 ## Internal wire primitives and entropy tables. Pure Nim; no foreign codec.
 ## FSE/Huffman decoding follows Go internal/zstd (BSD-3-Clause, see LICENSE).
-import std/bitops
+import std/[bitops, endians]
 
 type
   ZstdError* = object of CatchableError
@@ -27,8 +27,15 @@
 proc mask*(n: int): uint64 {.inline.} = (1'u64 shl n) - 1
 
 proc readLe*(data: openArray[char], pos: var int, n: int): uint64 {.inline.} =
-  require(pos >= 0 and n <= data.len - pos, "truncated input")
-  for i in 0..<n: result = result or (uint64(ord(data[pos+i])) shl (8*i))
+  require(pos >= 0 and n >= 0 and n <= data.len - pos, "truncated input")
+  case n
+  of 8: littleEndian64(addr result, unsafeAddr data[pos])
+  of 4:
+    var v: uint32
+    littleEndian32(addr v, unsafeAddr data[pos])
+    result = uint64(v)
+  else:
+    for i in 0..<n: result = result or (uint64(ord(data[pos+i])) shl (8*i))
   pos += n
 
 proc putLe*(dst: var string, x: uint64, n: int) {.inline.} =
@@ -41,6 +48,12 @@
   result = ReverseBits(pos: stop-1, start: start, count: floorLog(last), bits: uint64(last))
 
 proc fetch*(r: var ReverseBits, data: openArray[char], n: int): bool {.inline.} =
+  # Callers request at most 31 bits; a 32-bit refill fits the 64-bit cache.
+  if r.count < n and r.pos-r.start >= 4:
+    var p = r.pos-4
+    r.bits = (r.bits shl 32) or readLe(data, p, 4)
+    r.pos -= 4
+    r.count += 32
   while r.count < n and r.pos > r.start:
     dec r.pos
     r.bits = (r.bits shl 8) or uint64(ord(data[r.pos]))
--- a/src/zstd/encode.nim
+++ b/src/zstd/encode.nim
@@ -1,5 +1,6 @@
 ## Greedy LZ77 with a 64 KiB hash table and predefined FSE codes.
 import common, xxhash
+import std/endians
 
 type
   Sequence = object
@@ -12,14 +13,17 @@
 proc write(w: var BitWriter, value, n: int) {.inline.} =
   w.bits = w.bits or ((uint64(value) and mask(n)) shl w.count)
   w.count += n
-  while w.count >= 8:
-    w.bytes.add char(w.bits and 255)
-    w.bits = w.bits shr 8
-    w.count -= 8
+  if w.count >= 32:
+    let old = w.bytes.len
+    w.bytes.setLen(old+4)
+    var word = uint32(w.bits and 0xffffffff'u64)
+    littleEndian32(addr w.bytes[old], addr word)
+    w.bits = w.bits shr 32
+    w.count -= 32
 
 proc finish(w: var BitWriter) =
   w.write(1, 1)
-  if w.count > 0: w.bytes.add char(w.bits and 255)
+  w.bytes.putLe(w.bits, (w.count+7) shr 3)
 
 proc inverse(table: FseTable): array[53, array[64, uint16]] =
   for i in 0..<1 shl table.log:
@@ -33,6 +37,7 @@
 
 proc symbol(value: int, bases: openArray[int]): int {.inline.} =
   # Tiny length tables; binary search avoids scans for long matches.
+  if value < bases[16]: return value-bases[0]
   var lo = 0
   var hi = bases.len
   while lo+1 < hi:
@@ -45,34 +50,36 @@
   w.write(state, int(entry shr 6))
   state = int(entry and 63)
 
-proc encodeSequences(sequences: seq[Sequence]): string =
+proc encodeSequences(dst: var string, sequences: seq[Sequence]) =
+  var w = BitWriter(bytes: move(dst))
   let n = sequences.len
-  if n < 128: result.add char(n)
+  if n < 128: w.bytes.add char(n)
   elif n < 0x7f00:
-    result.add char((n shr 8)+128)
-    result.add char(n and 255)
+    w.bytes.add char((n shr 8)+128)
+    w.bytes.add char(n and 255)
   else:
-    result.add char(255)
-    result.putLe(uint64(n-0x7f00), 2)
-  if n == 0: return
-  var w: BitWriter
+    w.bytes.add char(255)
+    w.bytes.putLe(uint64(n-0x7f00), 2)
+  if n == 0:
+    dst = move(w.bytes)
+    return
   if n == 1:
     let s = sequences[0]
     let lc = symbol(int(s.literal), LlBase)
     let mc = symbol(int(s.match), MlBase)
     let off = int(s.offset)+3
     let oc = floorLog(off)
-    result.add char(0x54) # One sequence needs only RLE symbols, no FSE states.
-    result.add char(lc)
-    result.add char(oc)
-    result.add char(mc)
+    w.bytes.add char(0x54) # One sequence needs only RLE symbols, no FSE states.
+    w.bytes.add char(lc)
+    w.bytes.add char(oc)
+    w.bytes.add char(mc)
     w.write(int(s.literal)-LlBase[lc], LlBits[lc])
     w.write(int(s.match)-MlBase[mc], MlBits[mc])
     w.write(off-(1 shl oc), oc)
     w.finish()
-    result.add w.bytes
+    dst = move(w.bytes)
     return
-  result.add char(0) # All three FSE tables use the predefined distribution.
+  w.bytes.add char(0) # All three FSE tables use the predefined distribution.
   var ls, os, ms: int
   for i in countdown(n-1, 0):
     let s = sequences[i]
@@ -97,7 +104,7 @@
   w.write(os, 5)
   w.write(ls, 6)
   w.finish()
-  result.add w.bytes
+  dst = move(w.bytes)
 
 proc word(data: openArray[char], p: int): uint32 {.inline.} =
   copyMem(addr result, unsafeAddr data[p], 4)
@@ -107,13 +114,20 @@
   if size < 32: dst.add char(size shl 3)
   elif size < 4096: dst.putLe(uint64((size shl 4) or 4), 2)
   else: dst.putLe(uint64((size shl 4) or 12), 3)
+
+proc addBlock(dst: var string, data: openArray[char], kind, last: int) =
+  let old = dst.len
+  dst.setLen(old+3+data.len)
+  let header = (data.len shl 3) or (kind shl 1) or last
+  for i in 0..2: dst[old+i] = char((header shr (8*i)) and 255)
+  if data.len > 0: copyMem(addr dst[old+3], unsafeAddr data[0], data.len)
 
 proc compress*(data: openArray[char], checksum = true): string =
   ## Produce standard Zstandard frames. Memory is bounded by one 128 KiB
   ## block plus match scratch and the returned string; input is never copied.
   ## ponytail: greedy block-local matches and raw literals favor speed/memory;
   ## add lazy matching/adaptive entropy when compression ratio matters more.
-  result = newStringOfCap(min(data.len, BlockSize)+32)
+  result = newStringOfCap(min(data.len, 256)+32)
   result.putLe(0xfd2fb528'u64, 4)
   let check = if checksum: 4 else: 0
   if data.len < 256:
@@ -133,7 +147,7 @@
     result.putLe(uint64(data.len), 8)
   var table: array[16384, uint32]
   var sequences: seq[Sequence]
-  var literals = newStringOfCap(min(data.len, BlockSize))
+  var literals: string
   var start = 0
   while true:
     let stop = min(data.len, start+BlockSize)
@@ -179,22 +193,18 @@
         else:
           inc misses
           p += 1+(misses shr 7)
-      let old = literals.len
-      literals.setLen(old+stop-anchor)
-      if stop > anchor: copyMem(addr literals[old], unsafeAddr data[anchor], stop-anchor)
       var encoded: string
       if sequences.len > 0:
+        let old = literals.len
+        literals.setLen(old+stop-anchor)
+        if stop > anchor: copyMem(addr literals[old], unsafeAddr data[anchor], stop-anchor)
         encoded.literalsHeader(literals.len)
         encoded.add literals
-        encoded.add encodeSequences(sequences)
+        encoded.encodeSequences(sequences)
       if sequences.len > 0 and encoded.len < size:
-        result.putLe(uint64((encoded.len shl 3) or 4 or last), 3)
-        result.add encoded
+        result.addBlock(encoded, 2, last)
       else:
-        result.putLe(uint64((size shl 3) or last), 3)
-        let outStart = result.len
-        result.setLen(outStart+size)
-        if size > 0: copyMem(addr result[outStart], unsafeAddr data[start], size)
+        result.addBlock(data.toOpenArray(start, stop-1), 0, last)
     if last != 0: break
     start = stop
   if checksum: result.putLe(xxh64(data) and 0xffffffff'u64, 4)
