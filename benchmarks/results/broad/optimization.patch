--- a/src/zstd/common.nim
+++ b/src/zstd/common.nim
@@ -61,7 +61,7 @@
   r.count >= n
 
 proc take*(r: var ReverseBits, data: openArray[char], n: int): int {.inline.} =
-  require(r.fetch(data, n), "truncated bitstream")
+  if r.count < n: require(r.fetch(data, n), "truncated bitstream")
   r.count -= n
   int((r.bits shr r.count) and mask(n))
 
--- a/src/zstd/decode.nim
+++ b/src/zstd/decode.nim
@@ -75,7 +75,7 @@
   var r = initReverse(data, start, stop)
   let log = d.huffLog
   for i in dest..<dest+size:
-    discard r.fetch(data, log)
+    if r.count < log: discard r.fetch(data, log)
     # Zero padding is only for lookup; the selected code must fit real bits.
     let index = if r.count >= log: int((r.bits shr (r.count-log)) and mask(log))
                 else: int((r.bits shl (log-r.count)) and mask(log))
@@ -86,7 +86,8 @@
     d.literals[i] = char(entry shr 8)
   require(r.finished, "extra Huffman bits")
 
-proc readLiterals(d: var Decoder, data: openArray[char], p: var int) =
+proc readLiterals(d: var Decoder, data: openArray[char], p: var int): tuple[offset, size: int] =
+  result.offset = -1
   let header = int(readLe(data, p, 1))
   let kind = header and 3
   let format = (header shr 2) and 3
@@ -97,12 +98,12 @@
     of 1: size = (header shr 4) or (int(readLe(data, p, 1)) shl 4)
     else: size = (header shr 4) or (int(readLe(data, p, 2)) shl 4)
     require(size <= BlockSize, "too many literals")
-    d.literals.setLen(size)
     if kind == 0:
       require(size <= data.len-p, "truncated raw literals")
-      if size > 0: copyMem(addr d.literals[0], unsafeAddr data[p], size)
+      result = (p, size)
       p += size
     else:
+      d.literals.setLen(size)
       let v = char(readLe(data, p, 1))
       for i in 0..<size: d.literals[i] = v
     return
@@ -156,16 +157,15 @@
     dst.setLen(old+n)
     copyMem(addr dst[old], unsafeAddr data[start], n)
 
-proc compressedBlock(d: var Decoder, data: openArray[char], dst: var string,
-                     frameStart, window, limit, blockLimit: int) =
+proc execSequences(d: var Decoder, data, literals: openArray[char], pos: int,
+                   dst: var string, frameStart, window, limit, blockLimit: int) =
   let blockStart = dst.len
   let stop = min(limit, dst.len+min(blockLimit, high(int)-dst.len))
-  var p = 0
-  d.readLiterals(data, p)
+  var p = pos
   var count = int(readLe(data, p, 1))
   if count == 0:
     require(p == data.len, "extra bytes after literals")
-    dst.appendBytes(d.literals, 0, d.literals.len, stop)
+    dst.appendBytes(literals, 0, literals.len, stop)
     return
   if count == 255: count = int(readLe(data, p, 2))+0x7f00
   elif count >= 128: count = ((count-128) shl 8)+int(readLe(data, p, 1))
@@ -205,8 +205,8 @@
       states[0] = int(le.base)+r.take(data, int(le.bits))
       states[2] = int(me.base)+r.take(data, int(me.bits))
       states[1] = int(oe.base)+r.take(data, int(oe.bits))
-    require(literal <= d.literals.len-litPos, "literal length overflow")
-    dst.appendBytes(d.literals, litPos, literal, stop)
+    require(literal <= literals.len-litPos, "literal length overflow")
+    dst.appendBytes(literals, litPos, literal, stop)
     litPos += literal
     require(offset > 0 and offset <= dst.len-frameStart and
       (offset <= window or offset <= dst.len-blockStart), "match offset outside window")
@@ -219,8 +219,18 @@
       let n = min(match-copied, offset+copied)
       copyMem(addr dst[old+copied], addr dst[old-offset], n)
       copied += n
-  dst.appendBytes(d.literals, litPos, d.literals.len-litPos, stop)
+  dst.appendBytes(literals, litPos, literals.len-litPos, stop)
   require(r.finished, "extra sequence bits")
+
+proc compressedBlock(d: var Decoder, data: openArray[char], dst: var string,
+                     frameStart, window, limit, blockLimit: int) =
+  var p = 0
+  let raw = d.readLiterals(data, p)
+  if raw.offset >= 0:
+    d.execSequences(data, data.toOpenArray(raw.offset, raw.offset+raw.size-1), p,
+                    dst, frameStart, window, limit, blockLimit)
+  else:
+    d.execSequences(data, d.literals, p, dst, frameStart, window, limit, blockLimit)
 
 proc decompress*(data: openArray[char], maxOutput = 256*1024*1024,
                  maxWindow = 128*1024*1024): string =
@@ -260,6 +270,8 @@
     if sizeBytes != 0:
       require(size <= uint64(maxOutput-result.len), "decompressed size limit exceeded")
       limit = result.len+int(size)
+      # The validated content size avoids geometric reallocations/copies.
+      if frameStart == 0 and size > 0: result = newStringOfCap(int(size))
     d.huffLog = 0
     for table in d.tables.mitems: table.valid = false
     d.reps = [1,4,8]
--- a/src/zstd/encode.nim
+++ b/src/zstd/encode.nim
@@ -10,7 +10,7 @@
     bits: uint64
     count: int
 
-proc write(w: var BitWriter, value, n: int) {.inline.} =
+template write(w: var BitWriter, value, n: int) =
   w.bits = w.bits or ((uint64(value) and mask(n)) shl w.count)
   w.count += n
   if w.count >= 32:
@@ -25,15 +25,15 @@
   w.write(1, 1)
   w.bytes.putLe(w.bits, (w.count+7) shr 3)
 
-proc inverse(table: FseTable): array[53, array[64, uint16]] =
+proc inverse[Symbols, States: static int](table: FseTable): array[Symbols, array[States, uint16]] =
   for i in 0..<1 shl table.log:
     let e = table.entries[i]
     for next in int(e.base)..<int(e.base)+(1 shl int(e.bits)):
       result[e.sym][next] = uint16(i or (int(e.bits) shl 6))
 const
-  EncodeLl = inverse(DefaultLl)
-  EncodeOf = inverse(DefaultOf)
-  EncodeMl = inverse(DefaultMl)
+  EncodeLl = inverse[36, 64](DefaultLl)
+  EncodeOf = inverse[29, 32](DefaultOf)
+  EncodeMl = inverse[53, 64](DefaultMl)
 
 proc symbol(value: int, bases: openArray[int]): int {.inline.} =
   # Tiny length tables; binary search avoids scans for long matches.
@@ -145,7 +145,9 @@
     result.add char(192 or check)
     result.add char(56)
     result.putLe(uint64(data.len), 8)
-  var table: array[16384, uint32]
+  # Loads use initialized presence bits or a full position-table clear below.
+  var table {.noinit.}: array[16384, uint32]
+  var seen {.noinit.}: array[256, uint64]
   var sequences: seq[Sequence]
   var literals: string
   var start = 0
@@ -162,7 +164,22 @@
       result.putLe(uint64((size shl 3) or 2 or last), 3)
       result.add data[start]
     else:
-      zeroMem(addr table[0], sizeof(table))
+      # Preserve the full hash and match choices for short messages, while
+      # clearing 2 KiB of presence bits instead of 64 KiB of positions.
+      let sparse = size < 1024
+      if sparse: zeroMem(addr seen[0], sizeof(seen))
+      else: zeroMem(addr table[0], sizeof(table))
+      template replaceSlot(h, position: int): uint32 =
+        block:
+          let index = h
+          var previous = 0'u32
+          if sparse:
+            let bit = 1'u64 shl (index and 63)
+            if (seen[index shr 6] and bit) != 0: previous = table[index]
+            seen[index shr 6] = seen[index shr 6] or bit
+          else: previous = table[index]
+          table[index] = uint32(position)
+          previous
       sequences.setLen(0)
       literals.setLen(0)
       var anchor = start
@@ -171,8 +188,7 @@
       while p+4 <= stop:
         let v = word(data, p)
         let h = hash(v)
-        let prev = start+int(table[h])-1
-        table[h] = uint32(p-start+1)
+        let prev = start+int(replaceSlot(h, p-start+1))-1
         if prev >= start and word(data, prev) == v:
           var length = 4
           while p+length+8 <= stop:
@@ -189,7 +205,7 @@
           p += length
           anchor = p
           misses = 0
-          if p >= start+2 and p+2 <= stop: table[hash(word(data, p-2))] = uint32(p-start-1)
+          if p >= start+2 and p+2 <= stop: discard replaceSlot(hash(word(data, p-2)), p-start-1)
         else:
           inc misses
           p += 1+(misses shr 7)
